defaults:
  snmp_version: "v2c"
  timeout_s: 2.5
  retries: 1
  target_budget_s: 10
  oids:
    - "sysUpTime.0"
    - "sysName.0"

targets:
  - name: "router1"
    ip: "10.0.0.1"
    community: "public"
    oids:
      - "ifOperStatus.1"

  - name: "router2"
    ip: "10.0.0.2"
    community: "public"
